// Create an array of num and take input from the user to add numbers to this array
let arr = [1, 2, 5, 7, 4];
let a = prompt("Enter a num: ")
a = Number.parseInt(a);
arr.push(a)
console.log(arr)

// Run this code in the browser and check the output