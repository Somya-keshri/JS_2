// Print "You can drive" or "You cannot drive" based on age being greater than 18 or not using ternary operator

let age = 17;
//if(age >= 18)
console.log((age >= 18) ? "You can drive" : "You cannot drive");